<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <ImageView
        android:id="@+id/guild_scheduled_event_date_view_icon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:importantForAccessibility="no"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>
    <TextView
        android:textAppearance="@style/UiKit.TextAppearance.Semibold"
        android:textColor="?attr/colorHeaderSecondary"
        android:ellipsize="end"
        android:id="@+id/guild_scheduled_event_date_view_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginLeft="8dp"
        android:layout_marginRight="8dp"
        android:maxLines="1"
        app:layout_constraintBottom_toBottomOf="@+id/guild_scheduled_event_date_view_icon"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/guild_scheduled_event_date_view_icon"
        app:layout_constraintTop_toTopOf="@+id/guild_scheduled_event_date_view_icon"/>
</androidx.constraintlayout.widget.ConstraintLayout>
